experiment_name: baseline_real
run_group: phase1
model_name: cosmocluster-minimal
dataset_name: synthetic-suite
dataset_version: v0.1
task_type: clustering
train_entrypoint: src/train.py
eval_entrypoint: src/evaluate.py
hyperparameters:
  epochs: 20
  learning_rate: 0.001
  batch_size: 64
seed: 42
tags:
  - baseline
  - phone-first
  - real
artifact_policy:
  save_checkpoints: true
  save_plotly: true
  save_figures: true
compute_target:
  backend: modal
  instance_type: cpu-small
report_options:
  include_plotly: true
  include_failure_section: true
  mobile_emphasis: true
